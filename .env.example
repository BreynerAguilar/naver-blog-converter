# Google Cloud Storage Configuration for Naver Blog Converter
# Copy this file to .env and customize with your own GCS settings

# GCS Project ID (REQUIRED)
# Find your project ID: https://console.cloud.google.com/
GCS_PROJECT_ID=your-gcp-project-id

# GCS Bucket Name (REQUIRED - will be created if doesn't exist)
# Use a globally unique name, e.g., your-name-blog-images
GCS_BUCKET_NAME=your-bucket-name

# GCS Location (OPTIONAL - default: Seoul/ASIA-NORTHEAST3)
# Common values: US, EU, ASIA-NORTHEAST3 (Seoul), ASIA-NORTHEAST1 (Tokyo)
# Full list: https://cloud.google.com/storage/docs/locations
GCS_LOCATION=ASIA-NORTHEAST3

# Bucket subfolder for blog images (OPTIONAL - default: blog-images)
# All images will be uploaded to: gs://your-bucket/blog-images/
GCS_SUBFOLDER=blog-images

# Optional: Path to service account JSON (if not using gcloud auth)
# If not set, uses Application Default Credentials (gcloud auth application-default login)
# GCS_CREDENTIALS_PATH=/path/to/service-account.json
