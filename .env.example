# Google Cloud Storage Configuration for Naver Blog Converter
# Copy this file to .env and customize for different projects

# GCS Project ID
GCS_PROJECT_ID=n8nprojects-444223

# GCS Bucket Name (will be created if doesn't exist)
GCS_BUCKET_NAME=n8nprojects-naverblog

# GCS Location (default: Seoul)
GCS_LOCATION=ASIA-NORTHEAST3

# Bucket subfolder for blog images
GCS_SUBFOLDER=naverblog

# Optional: Path to service account JSON (if not using gcloud auth)
# GCS_CREDENTIALS_PATH=/path/to/service-account.json
